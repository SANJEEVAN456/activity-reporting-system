# Activity System Workspace

- `frontend/`: React + Vite app, connected to backend APIs
- `backend/`: Express + MongoDB API (auth, reports, admin)
- `render.yaml`: Render Blueprint for deploying both services from one repo
